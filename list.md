왜이리 오래 걸려 그냥 니가 알려준거 다시 나열 해달라는건데
1순위 — 목록에 있는데 없는 것

# 컴포넌트 비고

6 라디오 버튼 CheckBox만 있고 없습니다. 가장 명백한 구멍
8 토글/스위치
57·58·17 배지 / 태그 / 칩 아래 별도 설명
64 Empty Data 없으면 화면마다 하드코딩됩니다
101 스켈레톤 Spinner와 용도가 다릅니다
21 프로그레스 바 Loading·Spinner는 있지만 진행률이 없습니다. 파일 업로드·배치 작업에 필수
52 디바이더 30분짜리인데 없으면 사방에 border-top이 박힙니다
11 스텝퍼(NumberInput) 수량·금액 입력
배지·태그·칩은 블로그에서 흩어져 있는데, 셋을 명확히 갈라두면 팀이 헷갈리지 않습니다:
Badge — 알림 점/카운트. 다른 요소에 붙는 장식
Tag — 분류 레이블. 읽기 전용 또는 삭제 가능
Chip — 인터랙티브. 필터 선택, 입력 토큰
실무에선 Tag와 Chip을 하나로 합치고 interactive prop으로 나누는 경우가 많습니다. Badge만 확실히 분리하세요.
2순위 — 어드민이면 곧 필요해집니다

# 컴포넌트 비고

40 브레드크럼 계층 깊은 어드민에 필수
16 트리 메뉴 JsonTree는 JSON 전용. 조직도·카테고리·권한용 범용 Tree는 별개
60·27·28·29·61 AppShell (Header + GNB/LNB/SNB + Footer) 이 다섯은 개별 컴포넌트가 아니라 하나의 앱 셸로 묶는 게 맞습니다
43 스플릿 뷰 JsonTree + 상세 같은 이분할 화면. AgGrid 마스터-디테일에도
3 리스트 박스 DropdownMulti가 커버하는지 확인 후 결정
13 슬라이더 어드민이면 우선순위 중간
44 타임 픽커 / 데이트레인지 DayPicker는 단일 날짜만. 목록 필터엔 기간 선택이 거의 항상 필요합니다
19 아코디언 Collapsible이 있으니 그룹 래퍼만 추가
만들 필요 없는 것 — 이미 있는 걸로 처리
이게 목록에서 가장 중요한 부분일 수 있습니다. 그냥 따라 만들면 컴포넌트가 쓸데없이 불어납니다.
53·54·55·56·26 (벤토/도너/케밥/미트볼/햄버거 메뉴) → 전부 아이콘 모양 차이일 뿐입니다. TxDropMenu의 trigger variant로 처리하세요. 5개 컴포넌트를 만들면 안 됩니다
34·35·36 (백드롭/라이트박스/딤드) → 컴포넌트가 아니라 오버레이 공통 레이어입니다. 지난번에 말씀드린 z-index·dismiss 통합 관리가 바로 이겁니다
31 (팝업) → Modal로 대체. 새 창 팝업은 이제 안 씁니다
24 (스낵바) → Toast에 action prop 추가하면 끝입니다
70 (드로워) → SlidePanel이 이미 커버
47·48 (플레이스홀더/레이블) → Input·Field의 prop
9 (스크롤바) → CSS scrollbar-color로 충분. JS 커스텀 스크롤바는 접근성만 깎아먹습니다
72 (MRU) → Combobox의 "최근 항목" 기능
12 (다이얼) → 음향 편집 툴용. 불필요
74 (호버) → 상태이지 컴포넌트가 아닙니다
지금은 보류
모바일 (나중에): 23 바텀시트, 25 앱바, 39 스와이프 페이징, 41 스와이프 뷰, 42 플립 뷰, 45 FAB, 62 온보딩, 63 스플래시, 65 코치마크, 66 워크스루, 67 런처, 68 스테이터스 바, 71 Pull to Refresh
어드민엔 안 맞음: 15 리본 메뉴, 20 캐러셀, 59 히어로 배너, 73 메가 드롭다운, 49 코멘트, 50 피드 — 마케팅 페이지나 소셜 서비스를 같이 만들 계획이 없으면 건너뛰세요
한 가지 덧붙이면, 이 블로그 목록은 앱·서비스 기획 관점이라 업무용 어드민에 정작 필요한 게 빠져 있습니다. 지난번 언급한 FileUpload, Descriptions(라벨-값 상세 조회), Transfer(권한 이동), 경량 Table, Popover, MaskedInput은 여기 목록에 없지만 실제로는 더 자주 쓰입니다. 두 목록을 합쳐서 보시는 게 좋습니다.
바로 시작할 순서를 하나로 정리하면:
라디오 → 스위치 → 배지/태그 → 디바이더 → EmptyState → 스켈레톤 → 프로그레스
앞의 넷은 각각 반나절이면 끝나고 즉시 여기저기서 쓰입니다. 이것부터 털고 트리·앱셸 같은 무거운 걸 잡는 게 효율적입니다.
1순위 중에 코드로 잡아드릴 것 있을까요? 아니면 이 백로그를 진행 상황 체크하는 문서로 만들어드릴까요?

번호 이름 순위 플랫폼 설명 이런식으로 알아서 정리 해줘
