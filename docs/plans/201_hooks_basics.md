# 2-1. `@txstack/hooks` 기본

- 요구사항: [200 @txstack/hooks](../requirements/200_hooks_library.md) — H1 · H2
- 상태: 대기

## 목표

훅 4종의 옵션을 빠짐없이 검증한다. **타입이 곧 API** 인 패키지라 런타임 테스트만으로는 부족하다.

## job

| ID    | job                                                 | 담당 | 완료 기준                                                            |
| ----- | --------------------------------------------------- | ---- | -------------------------------------------------------------------- |
| H1-01 | `useUrlQuery` 의 `encode` 옵션 테스트               | 🤖   | 인코딩 켠 상태의 URL 형태가 고정된다                                 |
| H1-02 | `urlKeys` 동작 테스트                               | 🤖   | defaults 에 없는 키를 URL 에서 읽어오는 경로가 검증된다              |
| H1-03 | `replace` 옵션 테스트 (히스토리 누적 여부)          | 🤖   | 뒤로가기 동작이 고정된다                                             |
| H1-04 | `afterParse`(deprecated) 처리 방침 결정             | 🤝   | 남길지 제거할지. 미배포라 제거가 가장 쌈                             |
| H1-05 | H1-04 결정 적용 + changeset                         | 🤖   | 결정대로 반영                                                        |
| H1-06 | `useSafePolling` 즉시 1회 실행 옵션이 필요한지 판단 | 🤝   | 현재는 첫 tick 까지 기다린다. 실사용에서 어색한지 확인               |
| H1-07 | `useObjectChanged` 의 비교 깊이 문서화 · 테스트     | 🤖   | 얕은 비교임이 테스트로 고정되고 문서에 명시된다                      |
| H1-08 | 타입 수준 회귀 테스트 패턴을 4종 전체로 확장        | 🤖   | 추론이 무너지면 `pnpm typecheck` 가 실패한다 (`useUrlQuery` 는 완료) |
| H1-09 | 훅 반환 형태 일관성 점검                            | 🤖   | `[state, setter]` 와 `{ start, stop }` 혼재가 의도된 것인지 정리     |

## 검증

- `pnpm check`
- playground `/hooks` 화면에서 URL 왕복 확인
- 결과는 `docs/verification/201_*.md`

## 비고

`H1-08` 이 이 트랙의 핵심이다. `useUrlQuery` 추론 결함은 **playground 가 타입 인자를 명시하고 있어서**
1차 검증을 통과했다. 나머지 3종에도 같은 사각이 있는지 봐야 한다.
