# 1-3. `@txstack/ui` 고도화

- 요구사항: [100 @txstack/ui](../requirements/100_ui_library.md) — U8
- 선행: **1-1 · 1-2 가 닫힌 뒤에 연다**
- 상태: 대기

## 목표

"동작한다" 에서 "범용 라이브러리다" 로 올린다. 남은 앱 가정을 걷어내고, 확장점을 연다.

## job

### 미이관 자산 회수

| ID    | job                                                     | 담당 | 완료 기준                                                   |
| ----- | ------------------------------------------------------- | ---- | ----------------------------------------------------------- |
| U3-01 | `useTxAgGridOption` 의 도메인 결합 지점 목록화          | 🤖   | 원본이 무엇을 하드코딩했는지 표로 나온다                    |
| U3-02 | 범용화 설계안 (제네릭 / 옵션 주입)                      | 🤝   | 공개 API 형태를 예제 코드로 먼저 적고 승인받는다            |
| U3-03 | 구현 + 테스트                                           | 🤖   | 도메인 필드명 0건. changeset 포함                           |
| U3-04 | `TxDropdownMulti` JSDoc 재검토 (chain-wallet 판 미반영) | 🧑   | **원본 저장소가 있는 PC 필요.** 현재 API 와 어긋나는지 확인 |

### 테마 시스템

| ID    | job                                         | 담당 | 완료 기준                                      |
| ----- | ------------------------------------------- | ---- | ---------------------------------------------- |
| U3-05 | `TxClass*` 공통 토큰 목록·용도 정리         | 🤖   | 어떤 토큰이 어디 쓰이는지 표가 나온다          |
| U3-06 | 토큰만 바꿔 브랜드 색을 바꿀 수 있는지 점검 | 🤝   | 가능하면 문서화, 불가능하면 무엇이 막는지 기록 |
| U3-07 | 다크모드 대비(contrast) 문제 컴포넌트 수정  | 🤖   | `U2-09` 에서 나온 목록이 닫힌다                |

### 접근성

| ID    | job                                    | 담당 | 완료 기준                                            |
| ----- | -------------------------------------- | ---- | ---------------------------------------------------- |
| U3-08 | 키보드 조작 점검 (Overlay 그룹)        | 🤝   | Tab/ESC/화살표 조작 가능 여부. 안 되는 목록이 나온다 |
| U3-09 | 포커스 트랩 (`TxModal`·`TxSlidePanel`) | 🤖   | 열린 동안 포커스가 밖으로 새지 않는다                |
| U3-10 | `aria-*` 속성 점검                     | 🤖   | role·label 이 필요한 곳에 있다                       |

### 크기 · 성능

| ID    | job                                      | 담당 | 완료 기준                                                      |
| ----- | ---------------------------------------- | ---- | -------------------------------------------------------------- |
| U3-11 | 코어 청크 구성 분석                      | 🤖   | 무엇이 104KB 를 차지하는지 표로 나온다                         |
| U3-12 | `framer-motion` 필수 peer 유지 여부 판단 | 🤝   | Overlay 만 쓰는 의존인데 전 소비자가 설치한다. 분리할지 정한다 |
| U3-13 | 불필요한 리렌더 점검 (`memo` 적용 지점)  | 🤖   | 개선 여지 목록이 나온다                                        |

## 검증

- `pnpm check` + 소비자 실검증(`0-3`) 통과
- 코어 청크에 무거운 의존 유입 0건 유지
- 결과는 `docs/verification/103_*.md`

## 비고

`U3-12` 는 영향이 크다 — `framer-motion` 을 optional 로 돌리면 Overlay 그룹을 서브패스로 옮겨야 할 수도 있다.
**1-1 · 1-2 가 끝난 뒤에** 판단한다. 지금 건드리면 카탈로그 구조가 흔들린다.
