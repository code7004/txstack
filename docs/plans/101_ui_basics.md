# 1-1. `@txstack/ui` 기본

- 요구사항: [100 @txstack/ui](../requirements/100_ui_library.md)
- 제품 정의: [000 §2-1](../requirements/000_product_definition.md) — **확정 (2026-08-19)**
- 상태: 대기

## 목표

제거하기로 한 것을 걷어내고, 알려진 결함을 0건으로 만들고, 공개 API 이름을 일관되게 맞춘다.

**이 트랙이 끝나기 전에는 1-3(고도화)으로 넘어가지 않는다** — 이름이 흔들리는 상태에서 기능을 얹으면
나중에 두 번 고친다.

## job

### A. 제거 (먼저)

`000 §2-1` 확정분. 둘을 빼면 패키지 소스의 **18%(1,029줄)** 가 줄어든다.

| ID    | job                                  | 담당 | 완료 기준                                                                             |
| ----- | ------------------------------------ | ---- | ------------------------------------------------------------------------------------- |
| U1-01 | `TxCoolTable` 제거 (7파일 · 1,024줄) | 🤖   | 배럴 export · 테스트 · 스토리 · playground 전시가 함께 사라진다                       |
| U1-02 | `TxCoolTable` 하위 유틸 처리         | 🤖   | `safeRender`·`castValue`·`getNestedValue`·`setNestedValue` 는 내부 전용이라 함께 제거 |
| U1-03 | `orderByKey` 존치 판단               | 🤝   | 우리 테스트만 참조한다. 정렬 유틸이 UI 라이브러리 소관인지 판단                       |
| U1-04 | `TxClipboardButton` 제거 (5줄)       | 🤖   | `copyToClipboard` 유틸도 소비자가 없어지면 함께 판단                                  |
| U1-05 | 제거분 changeset 작성                | 🤖   | 대체 방법(`TxAgGrid`)이 적힌다                                                        |

> `U1-01` 로 `001-2` I8(렌더마다 찍히는 deprecation 경고)이 함께 해소된다. 별도 수정이 필요 없다.

### B. 결함 수정

| ID    | job                                                   | 담당 | 완료 기준                                                       |
| ----- | ----------------------------------------------------- | ---- | --------------------------------------------------------------- |
| U1-06 | 렌더 본문에서 부수효과를 내는 컴포넌트 전수 점검      | 🤖   | `console.*`·전역 변형이 렌더 본문에 있는 곳이 0건임을 확인·기록 |
| U1-07 | `TxTooltip` vs 폴더 `TxToolTip` 이름 불일치 방침 결정 | 🤝   | 어느 쪽으로 통일할지와 근거. **미배포라 지금이 가장 쌈**        |
| U1-08 | U1-07 결정 적용 + changeset                           | 🤖   | export·폴더·테마·타입 이름이 한 방향으로 정렬된다               |
| U1-09 | playground `menu.tsx` 의 `TxToolTip` 표기 오타 수정   | 🤖   | 화면 설명 문구가 실제 export 이름과 일치한다                    |

### C. 공개 API 일관성

| ID    | job                                           | 담당 | 완료 기준                                             |
| ----- | --------------------------------------------- | ---- | ----------------------------------------------------- |
| U1-10 | props 인터페이스 이름 규칙 점검표 작성        | 🤖   | `I<컴포넌트명>Props` 를 따르지 않는 것 목록이 나온다  |
| U1-11 | U1-10 에서 나온 불일치 정리 + changeset       | 🤖   | 규칙 이탈 0건                                         |
| U1-12 | 테마 객체 이름 규칙 점검 (`Tx*Theme`)         | 🤖   | 이탈 목록이 나오거나 0건임이 확인된다                 |
| U1-13 | 내부 전용 모듈(`tx-ui.dom.ts`) 노출 여부 점검 | 🤖   | 배럴로 새지 않음을 확인하거나, 의도적 노출이면 문서화 |

### D. 렌더 회귀 테스트

| ID    | job                                                 | 담당 | 완료 기준                                         |
| ----- | --------------------------------------------------- | ---- | ------------------------------------------------- |
| U1-14 | 렌더 테스트 뼈대 1종 (`TxButton`)                   | 🤖   | 렌더·클릭·비동기 로딩 잠금이 테스트로 고정된다    |
| U1-15 | Form 그룹 렌더 테스트                               | 🤖   | 입력·체크·드롭다운의 값 변경 콜백이 검증된다      |
| U1-16 | Overlay 그룹 렌더 테스트                            | 🤖   | 열림/닫힘, ESC·바깥 클릭 경로가 검증된다          |
| U1-17 | Data 그룹 렌더 테스트                               | 🤖   | `TxJsonTree` 가 falsy 값을 숨기지 않음을 고정한다 |
| U1-18 | a11y 룰 2종(`anchor-is-valid`·`alt-text`) 복구 검토 | 🤝   | 켤 수 있으면 켜고, 못 켜면 이유가 문서에 남는다   |

### E. 라우터 결합 해소 (배포 전 필수)

`000 §3-5` 확정분. **25종 중 3종 때문에 UI 라이브러리 전체가 React Router 에 묶여 있다.**
Next.js · TanStack Router 소비자가 설치조차 못 하므로, §1-1 의 "진짜 공개 범용" 과 어긋난다.

| ID    | job                                              | 담당 | 완료 기준                                                   |
| ----- | ------------------------------------------------ | ---- | ----------------------------------------------------------- |
| U1-19 | `linkComponent` 주입 API 설계 (형태 승인)        | 🤝   | `TxThemeProvider` 가 받는 형태를 예제 코드로 먼저 적고 승인 |
| U1-20 | `TxCard` 의 `link` 를 주입 방식으로 전환         | 🤖   | `react-router-dom` import 가 사라진다                       |
| U1-21 | `TxDropMenu.LinkItem` 을 주입 방식으로 전환      | 🤖   | 위와 동일                                                   |
| U1-22 | `TxContextMenu` 옵션 `to` 를 주입 방식으로 전환  | 🤖   | 위와 동일                                                   |
| U1-23 | 미지정 시 폴백 동작 정의 · 구현                  | 🤖   | 라우터를 안 쓰면 `<a>` 로 떨어지거나 해당 기능이 비활성     |
| U1-24 | `react-router-dom` 을 peerDependencies 에서 제거 | 🤖   | `packages/ui/package.json` 에서 빠진다                      |
| U1-25 | 소비자 픽스처로 검증 (라우터 미설치)             | 🤖   | react-router 없이 `@txstack/ui` 설치·빌드가 통과한다        |
| U1-26 | changeset 작성                                   | 🤖   | breaking 이다. 마이그레이션 방법이 적힌다                   |

> `U1-25` 가 이 절의 증명이다. peer 에서 지우는 것만으로는 결합이 실제로 끊겼는지 알 수 없다.

## 검증

- `pnpm check`
- 브라우저에서 playground 12화면 콘솔 **경고까지** 0건 (에러만 보지 않는다)
- `npm pack` 크기가 제거분만큼 줄었는지 확인
- 결과는 `docs/verification/101_*.md`

## 비고

- **A 를 먼저 한다.** 지울 것을 남겨둔 채 이름을 맞추면 지운 것의 이름까지 맞추게 된다.
- `U1-03` · `U1-07` 은 판단 job 이다. 결정 전에 코드를 건드리면 되돌리는 비용이 든다.
- 원본 3개 저장소가 이 PC 에 있다 (`/w/Projects/`). 사용량 대조가 필요하면 직접 읽는다.
  단 [CLAUDE.md](../../CLAUDE.md) 대로 **수정하지 않는다**.
