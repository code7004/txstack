# 1-1. `@txstack/ui` 기본

- 요구사항: [100 @txstack/ui](../requirements/100_ui_library.md) — U1 · U2 · U3
- 상태: 대기

## 목표

알려진 결함을 0건으로 만들고, 공개 API 이름을 27종에서 일관되게 맞춘다.
**이 트랙이 끝나기 전에는 1-3(고도화)으로 넘어가지 않는다** — 이름이 흔들리는 상태에서 기능을 얹으면
나중에 두 번 고친다.

## job

### 결함 수정

| ID    | job                                                   | 담당 | 완료 기준                                                                |
| ----- | ----------------------------------------------------- | ---- | ------------------------------------------------------------------------ |
| U1-01 | `TxCoolTable` deprecation 경고를 프로세스당 1회로     | 🤖   | 화면 하나에서 4회 → 1회. `useEffect` 로 옮겨 렌더 본문 부수효과를 없앤다 |
| U1-02 | 렌더 본문에서 부수효과를 내는 다른 컴포넌트 전수 점검 | 🤖   | `console.*`·전역 변형이 렌더 본문에 있는 곳이 0건임을 확인·기록          |
| U1-03 | `TxTooltip` vs 폴더 `TxToolTip` 이름 불일치 방침 결정 | 🤝   | 어느 쪽으로 통일할지와 근거가 남는다 (**미배포라 지금이 가장 쌈**)       |
| U1-04 | U1-03 결정 적용 + changeset                           | 🤖   | export·폴더·테마·타입 이름이 한 방향으로 정렬된다                        |
| U1-05 | playground `menu.tsx` 의 `TxToolTip` 표기 오타 수정   | 🤖   | 화면 설명 문구가 실제 export 이름과 일치한다                             |

### 공개 API 일관성

| ID    | job                                           | 담당 | 완료 기준                                                          |
| ----- | --------------------------------------------- | ---- | ------------------------------------------------------------------ |
| U1-06 | 27종의 props 인터페이스 이름 규칙 점검표 작성 | 🤖   | `I<컴포넌트명>Props` 를 따르지 않는 것 목록이 나온다               |
| U1-07 | U1-06 에서 나온 불일치 정리 + changeset       | 🤖   | 규칙 이탈 0건                                                      |
| U1-08 | 테마 객체 이름 규칙 점검 (`Tx*Theme`)         | 🤖   | 이탈 목록이 나오거나 0건임이 확인된다                              |
| U1-09 | `TxCoolTable` deprecated 최종 처리 방침 결정  | 🤝   | 유지 / 별도 subpath 이동 / 제거 중 택일. 배포 전이라 선택지가 넓다 |
| U1-10 | 내부 전용 모듈(`tx-ui.dom.ts`) 노출 여부 점검 | 🤖   | 배럴로 새지 않음을 확인하거나, 의도적 노출이면 문서화              |

### 렌더 회귀 테스트

| ID    | job                                                 | 담당 | 완료 기준                                         |
| ----- | --------------------------------------------------- | ---- | ------------------------------------------------- |
| U1-11 | 렌더 테스트 뼈대 1종 (`TxButton`)                   | 🤖   | 렌더·클릭·비동기 로딩 잠금이 테스트로 고정된다    |
| U1-12 | Form 그룹 렌더 테스트                               | 🤖   | 입력·체크·드롭다운의 값 변경 콜백이 검증된다      |
| U1-13 | Overlay 그룹 렌더 테스트                            | 🤖   | 열림/닫힘, ESC·바깥 클릭 경로가 검증된다          |
| U1-14 | Data 그룹 렌더 테스트                               | 🤖   | `TxJsonTree` 가 falsy 값을 숨기지 않음을 고정한다 |
| U1-15 | a11y 룰 2종(`anchor-is-valid`·`alt-text`) 복구 검토 | 🤝   | 켤 수 있으면 켜고, 못 켜면 이유가 문서에 남는다   |

## 검증

- `pnpm check` 통과
- 브라우저에서 playground 12화면 콘솔 **경고까지** 0건 (에러만 보지 않는다)
- 결과는 `docs/verification/101_*.md`

## 비고

`U1-03` 과 `U1-09` 는 **판단이 먼저다.** 결정 전에 코드를 건드리면 되돌리는 비용이 든다.
둘 다 배포 후에는 breaking 이라 지금 정하는 게 가장 싸다.
