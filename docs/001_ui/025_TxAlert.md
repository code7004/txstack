# 025 · TxAlert

> 페이지 안에 박히는 안내 상자.

|             |                                                             |
| ----------- | ----------------------------------------------------------- |
| 진입점      | `@txstack/ui`                                               |
| 내보내는 것 | `TxAlert`                                                   |
| 소스        | [`packages/ui/src/TxAlert/`](../../packages/ui/src/TxAlert) |
| 테스트      | 38개                                                        |

## 개발 목적

화면에 박히는 안내. **상태색 네 갈래(`info` · `success` · `warning` · `danger`)의 기준**을 세우고, 그 어휘를 `TxToast` · `TxTag` · `TxBadge` 가 물려받는다.

## 기능

### 쓰는 법

```tsx
<TxAlert variant="danger">저장하지 못했습니다.</TxAlert>

<TxAlert variant="warning" title="결제 수단이 곧 만료됩니다">
  9월 30일 이후에는 자동 결제가 중단됩니다.
  <TxAlert.Actions>
    <TxButton label="카드 변경" />
  </TxAlert.Actions>
</TxAlert>
```

**뜨는 것이 아니라 자리를 차지한다.** 나타났다 사라지는 알림은 `TxToast` 가 맡고,
그쪽이 이 겉모습을 그대로 쓴다 — `variant` 어휘도 같다.

갈래는 **색과 아이콘과 글자 셋으로** 알린다. 색만으로 알리면 색을 못 보는 사람에게
아무것도 남지 않는다.

겉모습은 CSS 변수로 바꾼다 — `.tx-alert { --tx-alert-radius: 0 }`.

## 개발 항목

- [x] **구현** — `packages/ui/src/TxAlert/`
- [x] **테스트** — 38개
- [x] **스토리** — `TxAlert.stories.tsx`
- [x] **CSS 계약 테스트** — 토큰 · 다크 분기 없음 · `styles.css` 적재 · `@layer tx`

## 정한 것 · 고친 것

신규다. 옮길 원본이 없으므로 **결함 목록 대신 정한 것을 적는다.**

### 갈래는 색 하나만 갈린다

`--tx-alert-accent` 를 갈래가 정하고 **바탕 · 테두리 · 아이콘 색이 전부 거기서 섞여 나온다**
(`color-mix`). 그래서 갈래를 늘리거나 소비자가 색을 바꿀 때 손댈 곳이 한 군데고,
다크모드도 토큰이 뒤집히면 저절로 따라온다. 테스트가 이 규칙을 지킨다 —
갈래 규칙 안에 선언이 둘 이상이면 깨진다.

### 색만으로 뜻을 전하지 않는다

갈래는 **색 · 아이콘 · 글자 셋으로** 알린다. 색만 쓰면 색을 못 보는 사람과 스크린리더에
아무것도 남지 않는다 (WCAG 1.4.1). 화면에는 안 보이는 `안내:` · `완료:` · `주의:` ·
`오류:` 가 상자 앞에 실린다. **`display: none` 이 아니라 `clip-path` 로 감춘다** —
전자는 스크린리더에서도 사라져서 감추는 뜻이 없어진다. 이것도 테스트가 지킨다.

### 라이브 리전은 기본이 꺼져 있다 (판단)

`role="alert"` 을 늘 달면 **페이지에 처음부터 있던 안내까지 읽어 주어 읽는 흐름을 끊는다.**
그래서 `announce` 를 두고 기본을 끔으로 했다 — 저장 결과처럼 동작에 대한 답으로 새로
나타나는 알림에만 켠다. 켜면 `danger` 는 `role="alert"`(즉시), 나머지는 `role="status"`
(하던 말 뒤)로 갈린다. 이 갈림은 `TxToast` 가 그대로 물려받는다.
