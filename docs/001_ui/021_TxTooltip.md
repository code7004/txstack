# 021 · TxTooltip

> 올리면 뜨는 짧은 설명.

| | |
| --- | --- |
| 진입점 | `@txstack/ui` |
| 내보내는 것 | `TxTooltip` |
| 소스 | [`packages/ui/src/TxTooltip/`](../../packages/ui/src/TxTooltip) |
| 테스트 | 31개 |

## 개발 목적

올리면 뜨는 짧은 설명. 원본은 **마우스로만 열려** 키보드 사용자에게는 없는 것이었고, 설명을 받을 요소도 정해져 있지 않았다.

## 기능

```tsx
<TxTooltip tip="삭제하면 되돌릴 수 없다">
  <TxButton label="삭제" variant="danger" />
</TxTooltip>
```

**여는 길이 셋이다** — 마우스를 올리거나, 키보드로 포커스하거나, 터치로 길게 누른다.
닫는 것은 벗어나기 · Escape · 바깥 누르기다.

- `tip` 은 `ReactNode` 다. 표 안에서 JSON 트리를 띄우는 자리가 그렇다
- **툴팁 위로 마우스를 올려도 안 닫힌다.** 긴 내용을 읽거나 글자를 긁을 수 있다
- 감싼 내용이 이미 포커스를 받으면(버튼 등) **감싸개는 탭 순서에 끼어들지 않는다.**
  글자만 감쌌으면 감싸개가 대신 받는다 — 그래야 키보드로도 볼 수 있다

겉모습은 CSS 변수로 바꾼다 — `.tx-tooltip { --tx-tooltip-bg: … }`.

## 개발 항목

- [x] **구현** — `packages/ui/src/TxTooltip/`
- [x] **테스트** — 31개
- [x] **스토리** — `TxTooltip.stories.tsx`
- [x] **CSS 계약 테스트** — 토큰 · 다크 분기 없음 · `styles.css` 적재 · `@layer tx`

## 정한 것 · 고친 것

원본 125줄. **여는 길이 hover 하나뿐**이라 키보드로도 터치로도 열 수 없었고,
`role="tooltip"` 도 `aria-describedby` 도 없어 스크린리더에는 존재하지 않았으며,
Escape 로 치울 수도 없었다 (WCAG 1.4.13 이 요구하는 셋 중 어느 것도 없었다).

지금은 **여는 길이 셋**이다 — 마우스 올리기 · 포커스 · 터치로 길게 누르기.
**포커스로 열 때는 기다리지 않는다.** 키보드로 온 사람은 이미 그것을 보려고 온 것이다.

### 설명을 누가 받는가

`aria-describedby` 는 **포커스를 받는 그 요소**에 걸려야 읽힌다. 조상에 걸면 스크린리더가
읽지 않는다. 그런데 감싸는 대상이 버튼일 수도 있고 그냥 글자일 수도 있다.

그래서 마운트할 때 감싼 안에 포커스를 받는 것이 있는지 본다.

| 감싼 것        | 감싸개의 탭 순서 | 설명이 걸리는 곳 |
| -------------- | ---------------- | ---------------- |
| 버튼·링크·입력 | 끼어들지 않는다  | **그 요소**      |
| 그냥 글자      | 감싸개가 받는다  | 감싸개           |

버튼을 감쌌는데 감싸개까지 탭 순서에 넣으면 한 컨트롤에 정거장이 둘이 된다.
반대로 글자만 감쌌을 때 아무도 안 받으면 키보드만 쓰는 사람에게 툴팁이 존재하지 않는다.
**브라우저에서 둘 다 확인했다** — 버튼은 Tab 한 번에 오고 `aria-describedby` 가 버튼에 걸린다.
