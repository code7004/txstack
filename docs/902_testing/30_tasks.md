# 902 진행 보드 — 테스트

> 이 트랙은 **`S3` 단계의 양식을 소유**한다. 테스트 자체는 각 항목의 S3 에서 쓴다.
> 규칙: [06_COMPONENT_FLOW](../00_foundation/06_COMPONENT_FLOW.md)

## 새 창을 여는 법

```
docs/README.md 와 docs/902_testing/ 를 읽고 902-01 부터 진행해줘.
```

## job

| job      | 내용                                                                                                                                                                       | 담당 | 선행                      | 상태 |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------------------------- | ---- |
| `902-01` | **무엇을 테스트하지 않을 것인가**를 먼저 못박는다                                                                                                                          | 🤝   | —                         |      |
| `902-02` | 계층별 기준 — 유틸 / 훅 / 컴포넌트 / 빌드 산출물 각각 어디까지                                                                                                             | 🤝   | `902-01`                  |      |
| `902-03` | 파일럿 1차(`TxSpinner`) 테스트를 양식 골격으로 만든다. **선행 끝났다 — 실물 13개와 관찰이 [README](README.md) 에 있다. jsdom 으로는 D1 을 못 잡는다는 게 거기서 드러났다** | 🤝   | ~~`001-TxSpinner-S3`~~ ✅ |      |
| `902-04` | 컴포넌트 테스트 범위 확정 — 렌더? 접근성? 상호작용?                                                                                                                        | 🤝   | `902-03`                  |      |
| `902-05` | 커버리지 도구 도입 여부와 목표치 판단                                                                                                                                      | 🤝   | `902-04`                  |      |
| `902-06` | 확정된 양식을 `20_design.md` 로 승격                                                                                                                                       | 🤖   | `902-02`~`05`             |      |

## 현행 실태 (2026-08-25)

| 패키지       | 테스트                                                | 비고                                                                   |
| ------------ | ----------------------------------------------------- | ---------------------------------------------------------------------- |
| `ui`         | `tx-ui.utils.test.ts` · **`TxSpinner.test.tsx` 13개** | 컴포넌트 렌더 테스트 선례가 생겼다 (2026-08-25). 나머지 25종은 `test✗` |
| `hooks`      | `hooks.test.tsx` 1개                                  |                                                                        |
| `route-meta` | `utils.test.ts` 79행                                  |                                                                        |
| `network`    | `client.test.ts` 155행                                | 이 저장소에서 가장 충실하다 — **선례로 검토할 가치 있음**              |

- vitest `^3`, 설정은 루트 `vitest.config.ts` 소유
- 환경 분기: `network`·`route-meta` 는 node, `hooks` 는 jsdom. **`ui` 는 확장자로 가른다** — `*.test.ts`→node / `*.test.tsx`→jsdom
- **`901`(Storybook)과 중복되는 영역이 있다.** 겉모습 검증을 스토리로 볼 것인지 테스트로 볼 것인지 `902-04` 에서 가른다
