# 001 · `@txstack/ui`

**Tx\* React 컴포넌트. 목표는 두 가지 — 쉬운 사용법, 쉬운 커스터마이징.**

`react` / `react-dom` 은 peerDependency 다.

## 무엇을 해결하는가

세 프로젝트가 같은 모달, 같은 드롭다운, 같은 테이블을 각자 복사해 들고 있었다.
한 곳에서 버그를 고쳐도 나머지 둘은 그대로였다.

여기서 중요한 건 **그냥 옮기는 게 아니라는 것**이다. 원본 컴포넌트들은
앱 전역 타입·도메인 지식·`import.meta.env` 에 묶여 있다. **그걸 걷어내야 라이브러리가 된다.**

판정 기준: **네 번째 프로젝트가 설치해서 그대로 쓸 수 있는가.**

## 진입점 구조

무거운 선택적 의존은 **서브패스로 분리한다.** 루트 배럴을 import 한 소비자는
`ag-grid` 나 `react-day-picker` 를 설치하지 않아도 동작해야 한다.

| 진입점                   | 내용               | 추가 peer 의존                      | 상태     |
| ------------------------ | ------------------ | ----------------------------------- | -------- |
| `@txstack/ui`            | 대부분의 컴포넌트  | 없음                                | 4개 있음 |
| `@txstack/ui/aggrid`     | `TxAgGrid`         | `ag-grid-community` `ag-grid-react` | 빈 배럴  |
| `@txstack/ui/daypicker`  | `TxDayPicker` 계열 | `react-day-picker` `dayjs`          | 빈 배럴  |
| `@txstack/ui/styles.css` | 전체 스타일시트    | —                                   | **있음** |

`tokens.css` 는 별도 진입점이 아니다 — `styles.css` 가 `@import` 로 먼저 싣는다.
컴포넌트를 옮길 때마다 `styles.css` 에 `@import` 가 한 줄씩 늘어난다.

```tsx
import { TxButton, TxModal } from "@txstack/ui";
import { TxAgGrid } from "@txstack/ui/aggrid";
import "@txstack/ui/styles.css";
```

## 이행 계획

temp 에 26개가 있다. **전부 이식하지 않는다.**

### 0차 — 스타일 파이프라인 · 완료

`tokens.css` · `styles.css` · `scripts/build-css.mjs` · `@txstack/ui/styles.css` 진입점.
빌드가 `@import` 를 펼쳐 `dist/styles.css` 하나로 내고, 그 내용을 `@layer tx { … }` 로 감싼다.
**감싸지 않으면 배포본만 레이어 밖으로 나와** 소비자의 `className` 이 안 먹는다.

### 1차 — 기반 4개 · 완료 (244줄 · 테스트 107개)

**temp 에서 CSS + 토큰 이행이 이미 끝난 것들이었다.** 거의 그대로 왔다.

| 순  | 컴포넌트    | 줄  | 의존        | 왜 먼저인가                    |
| --- | ----------- | --- | ----------- | ------------------------------ |
| 1   | `TxSpinner` | 57  | —           | `TxButton` 이 로딩 표시로 쓴다 |
| 2   | `TxButton`  | 84  | `TxSpinner` | **이행 기준이자 레퍼런스**     |
| 3   | `TxFlex`    | 33  | —           | `TxInput` 이 쓴다              |
| 4   | `TxLoading` | 70  | `TxSpinner` | 이행이 끝나 있어 싸다          |

`TxButton` 이 표준을 정한다 — `.tx-button` 클래스 + `data-variant`/`data-loading` 속성으로
스타일 훅을 노출하고, 색·반경은 `--tx-button-*` 이 전역 토큰을 기본값으로 받는다.

함께 온 것: `tx-ui.utils` 의 `cm()` **하나뿐이다.** temp 의 `themeMerge` · `getItemKey` ·
`copyToClipboard` 는 각각 `TxTheme`(폐기) · `TxDropdown` · `TxClipboardButton` 것이라
그 컴포넌트가 올 때 함께 온다. `getItemKey` 는 배럴을 import 하는 순환 참조라 그때 정리한다.

**테스트가 CSS 계약까지 본다.** jsdom 에는 스타일시트가 없어 렌더 결과로는 안 보이므로,
`.css` 파일을 텍스트로 읽어 검사한다 — 색을 하드코딩하지 않았는지, 다크모드 분기를
컴포넌트가 갖지 않는지, `styles.css` 에 실려 나가는지, `tx` 레이어 안으로 들어가는지.
**이 방식을 이후 컴포넌트에도 유지한다.**

### 2차 — Form 클러스터 (8개 · 1,942줄)

**순서는 의존 그래프가 정한다.** 잎부터 올라간다.

| 순  | 컴포넌트          | 줄      | 의존               | 비고                                           |
| --- | ----------------- | ------- | ------------------ | ---------------------------------------------- |
| 5   | `TxIcons`         | 79      | —                  | `TxInput` 이 필요하다                          |
| 6   | `TxInput`         | 307     | `TxFlex` `TxIcons` | `TxSearchInput` · `TxInputLike` 포함           |
| 7   | `TxTextarea`      | 101     | `TxInput`          |                                                |
| 8   | `TxCheckBox`      | 81      | —                  | `TxForm.CheckBox` 를 만들지 함께 본다          |
| 9   | `TxCapsLockCheck` | 59      | —                  | 비밀번호 입력                                  |
| 10  | `TxDropdown`      | **681** | `TxButton`         | 가장 크다. 순서대로 그대로 이식한다            |
| 11  | `TxDayPicker`     | 394     | `TxButton`         | **서브패스.** 분리를 실제로 검증하는 첫 케이스 |
| 12  | `TxForm`          | 240     | 위 전부            | 조립 계층                                      |

`TxForm` 은 `TxFormBase` 에 `Field` · `Flex` · `Label` · `Input` · `SearchInput` ·
`Textarea` · `Dropdown` · `DropdownMulti` 를 `Object.assign` 으로 묶은 네임스페이스다.

`TxForm.DayPicker` / `DayPickerRange` 는 **루트 배럴에 없다.** `@txstack/ui/daypicker` 가
가져간다 — 코어가 `react-day-picker` 를 import 하면 optional peer 가 성립하지 않는다.
이 분리는 temp 에서 이미 지켜져 있다.

### 제외 확정 — 2개

- **`TxCoolTable`**(1024줄) — 가져오지 않는다. 테이블은 `TxAgGrid` 가 맡는다.
- **`TxTheme`**(29줄) — 폐기. Tailwind 클래스 문자열 상수였다. 아래 4번.

### 3차 — 아직 정하지 않은 12개

| 분류        | 컴포넌트                                                                                 |
| ----------- | ---------------------------------------------------------------------------------------- |
| 필요할 듯   | `TxModal`(88) · `TxTabs`(107) · `TxToolTip`(133) · `TxSlidePanel`(154) · `TxCard`(166)   |
| 규모가 큼   | `TxAgGrid`(670) · `TxLayout`(398)                                                        |
| 메뉴 2종    | `TxDropMenu`(213) · `TxContextMenu`(165) — 아래                                          |
| 범용성 의심 | `TxJsonTree`(244, 디버깅 도구) · `TxHeader`(25, 사실상 styled div) · `TxClipboardButton` |

#### 메뉴 2종은 서로 다른 것이다

앞서 "역할 겹침" 으로 분류했는데 **틀렸다.** 트리거도 위치 계산도 다르다.

- **`TxDropMenu`** — GNB 에서 유저네임 같은 걸 누르면 **아래로 펼쳐지는** 메뉴
- **`TxContextMenu`** — 특정 태그에서 **마우스 오른쪽**을 누르면 나오는 메뉴 창

겹치는 건 공개 API 가 아니라 **내부 구현**이다 — 둘 다 `createPortal` · 뷰포트 클램핑 ·
바깥 클릭 닫기를 각자 들고 있다. 합칠지는 **둘 다 옮겨 본 뒤에** 고민한다.
지금 합치면 아직 모르는 차이를 추상화로 덮게 된다.

#### 먼저 풀어야 할 문제

`TxDropMenu` 와 `TxContextMenu` 가 `react-router-dom` 의 `NavLink` 를 직접 import 한다.
그런데 `@txstack/ui` 의 peer 에는 `react-router-dom` 이 없다. 이대로 옮기면 루트 배럴이
라우터를 끌어와 **Next.js·TanStack Router 소비자가 이 패키지를 못 쓴다.**
링크 컴포넌트를 prop 으로 주입받게 고치는 쪽이 맞아 보인다 — 옮길 때 판단한다.

## 커스터마이징 방식 — 결정됨

**CSS 커스텀 프로퍼티 토큰(`--tx-*`) + `tx` 캐스케이드 레이어.** temp 에서 이미 결론이 났고
`TxSpinner` · `TxButton` · `TxLoading` · `TxFlex` 4개가 이 방식으로 이행됐다. **다시 논의하지 않는다.**

### 1. 토큰이 커스터마이징의 정문이다

소비자는 `tokens.css` 의 전역 토큰만 덮으면 된다.

```css
:root {
  --tx-color-primary: #7c3aed;
  --tx-radius: 9999px;
}
```

컴포넌트 토큰(`--tx-button-bg` 등)이 이 값을 **기본값으로 받는다.** 그래서 한 줄만 바꾸면
`hover` · `focus` · `.dark` 가 저절로 따라온다 — 상태별로 하나씩 덮을 필요가 없다.

**토큰 이름은 공개 API 다.** 지우거나 바꾸면 major 다. 그래서 **안 쓰는 토큰을 미리 만들지 않는다.**
실제로 쓰는 컴포넌트가 생길 때 한 줄씩 늘린다.

temp 기준 전역 토큰은 11개다 — `--tx-color-{primary,danger,muted,state,text,on-accent}` ·
`--tx-state-{hover,pressed}` · `--tx-focus-ring` · `--tx-focus-ring-offset` · `--tx-radius`.

### 2. 스타일시트는 하나만 낸다

```tsx
import "@txstack/ui/styles.css";
```

**컴포넌트가 자기 CSS 를 import 하지 않는다.** `styles.css` 에서만 모은다 —
그래야 SSR·번들러 환경에 따라 깨지는 경로가 생기지 않는다. 빌드가 `@import` 를 펼쳐
`dist/styles.css` 하나로 낸다. **순서가 곧 캐스케이드다** — 토큰 → 컴포넌트 순.

### 3. `@layer tx` 가 `className` 을 살린다

전부 `tx` 레이어에 넣는다. 이 한 줄이 핵심이다.

- **레이어 밖 CSS 는 레이어 안 CSS 를 특이도와 무관하게 무조건 이긴다.**
  그래서 순수 CSS · Sass · CSS Modules 를 쓰는 소비자는 **아무것도 안 해도** 덮어쓸 수 있다.
- **Tailwind 를 쓴다면 한 줄이 필요하다.** `tx` 는 preflight(`base`) 뒤, 유틸리티 앞이어야 한다.
  앞에 두면 preflight 가 버튼 배경·여백을 지우고, 뒤에 두면 `className` 이 안 먹는다.

  ```css
  @layer theme, base, tx, components, utilities;

  @import "tailwindcss";
  @import "@txstack/ui/styles.css";
  ```

### 4. `TxTheme` 은 폐기됐다

`TxClassBase` 같은 Tailwind 클래스 문자열 상수였다. 공개 API 로 두면 소비자가 그걸 받아서
할 수 있는 일이 **Tailwind 를 쓰는 것뿐**이라, "소비자가 무엇을 쓰든 상관없다" 는 전제와 어긋난다.

**이식 시 `TxTheme` 을 참조하는 컴포넌트는 그 참조를 CSS + 토큰으로 바꾼 뒤에 가져온다.**
`TxTheme` 자체는 새 저장소로 옮기지 않는다.

## 이식 시 반드시 걷어낼 것

각 컴포넌트를 열 때 이것부터 확인한다.

- **`TxTheme` / Tailwind 클래스 문자열** — 위 4번. CSS + `--tx-*` 토큰으로 옮긴다.
- **앱 전역(ambient) 타입** — 소비자 프로젝트에는 그 전역이 없다.
- **도메인 지식** — 블랙/문자/지갑/유저타입 등. props 로 받거나 잘라낸다.
- **`import.meta.env` 직접 참조** — Vite 아닌 소비자에서 깨진다. 옵션으로 받는다.
- **다른 Tx\* 컴포넌트로의 숨은 의존** — 배럴 import 라면 순환이 생길 수 있다.
- **default export** — 배럴의 `export *` 는 default 를 실어 나르지 않는다. named 로 바꾼다.

## 결정할 것

- [ ] `TxForm.CheckBox` 를 만들지. `TxCheckBox` 를 옮길 때 함께 본다.
- [ ] `cm()` 이 `clsx` + `tailwind-merge` 다. CSS 이행이 끝나면 `twMerge` 가 할 일이 거의 없다 —
      `tailwind-merge` 의존을 뺄 수 있는지 Form 클러스터가 끝나면 본다.
- [ ] `TxInputLike` 같은 내부 조립 부품을 공개 API 로 낼지, 감출지. `TxInput` 을 옮길 때.
- [ ] 2차 18개 중 무엇을 자를지. Form 클러스터가 끝난 뒤에 정한다.
