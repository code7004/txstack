# CLAUDE.md

txstack 작업 시 지켜야 할 핵심 규약. **상세는 `docs/` 가 소유한다. 이 문서는 얇게 유지한다.**

## 시작하기

**작업을 이어받는다면 [`docs/README.md`](docs/README.md) 를 먼저 읽는다.**
무엇을 만드는지, 어디까지 왔는지, 다음에 뭘 할 차례인지가 거기 있다.

| 무엇을 하려는가             | 어디를 읽나                                              |
| --------------------------- | -------------------------------------------------------- |
| 전체 파악 · 다음 할 일 찾기 | [docs/README.md](docs/README.md)                         |
| 패키지 하나의 공개 API      | `docs/00X_<패키지>.md`                                   |
| 이전 구현 참고              | `../txstack_temp` — 리셋 전 상태의 별도 클론 (읽기 전용) |

## 프로젝트 개요

**여러 프로젝트에서 재사용할 수 있는 범용 React 라이브러리 세트.** npm 에 배포한다.
pnpm workspace 모노레포. `packages/*` 4종이 각각 독립 배포되고, `apps/*` 는 배포하지 않는다.

| 번호  | 패키지                | 역할                                             | React 의존 |
| ----- | --------------------- | ------------------------------------------------ | ---------- |
| `001` | `@txstack/ui`         | Tx\* UI 컴포넌트 — 쉬운 사용법·쉬운 커스터마이징 | O          |
| `002` | `@txstack/route-meta` | 라우트를 메타데이터로 관리 (icon·path·권한)      | O          |
| `003` | `@txstack/hooks`      | 범용 React 훅 + URL 쿼리 상태                    | O          |
| `004` | `@txstack/axios`      | axios 래퍼 — 정책 주입식 HTTP 클라이언트         | **X**      |

**현재 상태: 저장소를 비우고 다시 시작했다.** 이전 구현 전체는 원격의 **`legacy` 브랜치**에
53개 커밋과 함께 보존되어 있다. 지금 하는 일은 백지에서 새로 짜는 것이 아니라,
**거기서 하나씩 열어보고 정리해 이식하는 것**이다.

읽을 원본은 **`../txstack_temp` 에 별도 클론으로 놓는다.** 워크트리가 아니다 —
자기 `.git` 을 가진 독립 저장소라 이 저장소의 `git status` 나 도구가 그 폴더를 아예 보지 않는다.

```sh
git clone --branch legacy https://github.com/code7004/txstack.git ../txstack_temp
```

**`../txstack_temp` 는 읽기만 한다.** 거기에 커밋하지 않는다.

## 절대 규칙

작업 방식:

- **전체 틀은 사용자가 숙지한다.** "다 만들어" 가 아니다. 설계는 함께 합의하고, **첫 구현은 사용자가 한다.**
  Claude 는 반복 구현·테스트·정리를 맡는다.
- **판단이 필요한 작업은 사용자 승인 없이 넘어가지 않는다. 공개 API 형태는 예제 코드로 먼저 합의한다.**
- **커밋 / 푸시는 사용자가 명시적으로 요청할 때만.** 자동 커밋 금지.
- **`npm publish` 는 사용자가 명시적으로 요청할 때만.** 되돌릴 수 없다.
- **`txstack_temp` 는 읽기만 한다.** 수정·삭제하지 않는다.
- 기존 사용자 변경사항을 되돌리지 않는다. 관련 없는 diff 는 건드리지 않는다.
- **작업 단위는 컴포넌트·기능 하나다.** 계층별로 몰아서 하지 않는다.
- **vitest = 자동 검증 / Storybook = 사용자가 직접 보는 자리.** Storybook 에 `play` 함수나 `addon-vitest` 를 넣지 않는다.

패키지 경계 (어기면 범용 라이브러리가 아니다):

- **의존 방향은 `ui → hooks` 하나만 허용한다.** `hooks` / `route-meta` / `axios` 는 서로 참조하지 않는다.
- **앱 전역(ambient) 타입·전역 변수에 의존하지 않는다.** 소비자 프로젝트에는 그 전역이 없다.
- **런타임 정책(인증 토큰, 401 처리, 응답 봉투)을 패키지가 결정하지 않는다.** 옵션으로 주입받는다.
- **`react` / `react-dom` / `react-router-dom` / `ag-grid-*` / `axios` 는 peerDependencies 다.**
- **무거운 선택적 의존은 서브패스로 분리한다.** 루트 배럴을 import 해도 로드되지 않아야 한다.
- 도메인 지식(블랙/문자/지갑/유저타입 등)을 패키지 코드에 넣지 않는다.

환경:

- **pnpm 만 사용한다.** npm/yarn lockfile 을 만들지 않는다. Node `24.14.0`, pnpm `>=10.0.0`.
- ESLint / Prettier 설정은 **루트가 소유**한다. 패키지 개별 설정을 추가하지 않는다.
- 커밋 메시지는 Conventional Commits + 한글 (예: `feat: TxDropdown 다중선택 지원`).
- **원본 3개 저장소(`black-message` · `usertics` · `chain-wallet-service`)를 수정하지 않는다.** 읽기만 한다.

## 컴포넌트 이식 순서

**작업 단위는 컴포넌트 하나다.** 하나를 아래 순서로 끝까지 관통한 뒤 다음으로 간다.
계층별로 몰아서 하지 않는다.

| 단계 | 하는 일                                                                                          | 담당 |
| ---- | ------------------------------------------------------------------------------------------------ | ---- |
| 0    | **대상 선정** — [docs/001_ui.md](docs/001_ui.md) 의 이행 계획 순서를 따른다. 의존이 먼저다       | 🤖   |
| 1    | **원본 전체 읽기** — 옮기기 전에 다 읽는다. 경계 위반·결함·사실이 아닌 주석을 먼저 찾아 보고한다 | 🤖   |
| 2    | **`TxTheme` · Tailwind 클래스 → CSS + `--tx-*` 토큰**                                            | 🤖   |
| 3    | **S1 문서** — 공개 API 를 **예제 코드로** 합의한다                                               | 🤝   |
| 4    | **S2 구현**                                                                                      | 🤖   |
| 5    | **S3 테스트** — 동작 + **CSS 계약**                                                              | 🤖   |
| 6    | **S4 스토리** — `packages/ui/src/<Name>/<Name>.stories.tsx`                                      | 🤖   |
| 7    | **확인** — `pnpm storybook:dev` 로 직접 본다. **여기서 멈춘다**                                  | 🧑   |
| 8    | 지시가 오면 2차 개선                                                                             | 🤖   |

**판단이 필요하면 3단계에서 멈추고 묻는다.** 옮긴 뒤에 고치면 무엇이 원본이고 무엇이 판단이었는지
구분되지 않는다. 고친 내역은 각 패키지 문서의 "이식하며 고친 것" 에 남긴다.

**7단계를 건너뛰지 않는다.** 사용자가 Storybook 에서 보고 지시해야 다음 컴포넌트로 간다.

### 스토리 문구는 소비자 것이다

**Storybook 은 `@txstack/ui` 를 쓰는 사람이 보는 카탈로그다.** 이전 구현과의 비교,
결함 이력, 내부 구현 설명을 쓰지 않는다.

- ❌ "원본은 `onChangedText` 하나가 값 변경·Enter·blur 세 곳에서 불렸다"
- ✅ "`onChangeText` 는 값이 바뀔 때마다 온다. Enter 로 줄을 바꿔도 한 번만 온다"
- ❌ "`getValue` 는 DOM 에서 직접 읽어 항상 최신이다"
- ✅ "`getValue` 는 언제 불러도 지금 화면에 있는 값을 준다"

**무엇이 달라졌는지가 아니라 무엇을 할 수 있는지를 쓴다.** 이식 이력과 고친 내역은
`docs/` 와 커밋 메시지가 가진다. 소스 주석에는 남겨도 된다 — 그건 유지보수하는 사람 것이다.

스토리 이름도 마찬가지다. 내부 구조 용어(`SharesShell…`)가 아니라 보이는 것으로 짓는다.

### Storybook 이 안 바뀔 때

**타입 전용 import 는 Vite 의 모듈 그래프에 안 잡힌다.** 그래서 `*.types.ts` 를 고쳐도
HMR 이 docgen 을 다시 돌리지 않고, **문서 페이지의 props 표가 옛것으로 남는다.**
props 설명을 고쳤으면 Storybook 을 재시작한다.

`.css` 나 `.tsx` 를 고친 것은 HMR 로 반영된다.

### CSS 계약 테스트

jsdom 에는 스타일시트가 없어 렌더 결과로는 안 보인다. 그래서 `.css` 파일을 **텍스트로 읽어**
검사한다. `TxButton` · `TxFlex` 의 테스트가 본보기다.

- 색을 하드코딩하지 않고 토큰으로 내보내는가
- 다크모드 분기를 컴포넌트가 갖지 않는가 (토큰만 재정의하면 따라와야 한다)
- `styles.css` 에 실려 나가는가 (안 실리면 소비자에게 도달하지 않는다)
- `tx` 레이어 안으로 들어가는가 (레이어 밖이면 `className` 이 안 먹는다)
- 참조하는 전역 토큰이 전부 `tokens.css` 에 정의돼 있는가

## 검증 명령

```sh
pnpm check           # lint + typecheck + test  ← 기본
pnpm test            # 회귀 테스트 (vitest)
pnpm build           # packages/* 전체 빌드 (ui 는 dist/styles.css 까지)
pnpm storybook:dev   # 컴포넌트 카탈로그 — 사용자가 직접 보는 자리
```
