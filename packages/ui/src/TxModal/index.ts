/**
 * `TxModalBase` 와 `lockPageScroll` 은 내부 부품이라 내보내지 않는다.
 * 버튼 줄은 `TxModal.Footer` 로 닿는다.
 */
export { TxModal } from "./TxModal";

export type { TxModalFooterProps, TxModalProps, TxModalSize } from "./TxModal.types";
