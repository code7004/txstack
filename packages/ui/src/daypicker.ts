/**
 * `@txstack/ui/daypicker` — react-day-picker 를 필요로 하는 컴포넌트.
 *
 * `react-day-picker` / `dayjs` 는 **optional peerDependency** 다.
 * 이 서브패스를 import 하는 소비자만 설치하면 된다.
 *
 * **루트 배럴(`src/index.ts`)에서 이 파일을 참조하지 않는다.** 참조하는 순간 분리가 무너진다.
 */

export {};
