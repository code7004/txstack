/** `TxAlertBase` 와 아이콘·라벨 표는 내부 부품이라 내보내지 않는다. 버튼 줄은 `TxAlert.Actions` 로 닿는다. */
export { TxAlert } from "./TxAlert";

export type { TxAlertActionsProps, TxAlertProps, TxAlertVariant } from "./TxAlert.types";
