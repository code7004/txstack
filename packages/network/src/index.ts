/**
 * `@txstack/network` — axios 래퍼.
 *
 * **React 를 모른다.** 훅도 컨텍스트도 없다. Node 스크립트에서도 그대로 돌아야 한다.
 *
 * 인증 헤더·401 처리·응답 봉투는 **패키지가 결정하지 않는다.** 옵션으로 주입받는다.
 * 설계: docs/004_network.md
 *
 * 구현은 txstack_temp 에서 이식 중이다.
 */

export {};
