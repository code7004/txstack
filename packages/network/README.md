# @txstack/network

axios 래퍼. **인증·에러·응답봉투 정책을 옵션으로 주입받는다.**
React 를 모른다 — Node 스크립트에서도 그대로 돌아간다.

> **아직 배포되지 않았다.** 구현을 이식 중이다. 공개 API 초안은 [docs/004_network.md](../../docs/004_network.md).

```sh
pnpm add @txstack/network axios
```

`axios >=1.6` 은 peerDependency 다.
