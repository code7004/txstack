/**
 * `@txstack/hooks/router` — `react-router-dom` 을 필요로 하는 훅.
 *
 * `react-router-dom` 은 **optional peerDependency** 다. 이 서브패스를 import 하는 소비자만
 * 설치하면 된다.
 *
 * 설계: docs/003_hooks.md
 */

export {};
